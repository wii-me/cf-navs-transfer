# 开放 Issue 任务清单

> **性质：** GitHub 需求分析与建议任务快照，不是开发承诺，不维护第二份实时进度台账。
> **采集时间：** 2026-09-15 09:25:39 UTC。
> **范围：** `lbjxr/CF-Navs` 当前全部 Open Issue，共 **4 个**（#21、#19、#16、#15），已读取全部正文及 **7 条评论**；不包含 PR。#18 已 Closed / Completed，仅在文末保留历史注记。
> **本轮边界：** 只读调查、整理文档；未开发代码、运行项目测试或修改 GitHub 状态。
> **配套文档：** [开放 PR 审查任务清单](OPEN_PRS_TASK_LIST.md)。

## 1. 使用口径与总览

- 云端开闭状态、标签和排期以 GitHub 实时数据为准；下表只代表本次采集时刻。四个 Open Issue 均未设置 milestone 或 assignee，不能据此推断维护者已经排期。
- 「明确诉求」指报告者提出的内容，不等于维护者批准；「建议任务 / 验收」是本次分析形成的后续处理建议。
- 本地内部事项仍以 [BACKLOG](../BACKLOG.md) 为状态源；历史 R-01～R-08 的范围与验收见 [Issue 需求记录](../reference/GITHUB_ISSUES_REQUIREMENTS.md)，不因本轮查询重新列为待开发。

| Issue / 原标题 | 标签 / 云端状态 | 创建时间（UTC） | 最后更新（UTC） | 内容判断 | 建议下一步 |
| --- | --- | --- | --- | --- | --- |
| [#21](https://github.com/lbjxr/CF-Navs/issues/21) [Bug]: 详情风格下，隐藏描述可以正常生效，但是始终显示不生效 | `—` / Open | 2026-09-15 07:27:48 | 2026-09-15 07:27:48 | 正文仅说明详情风格下“隐藏描述”正常、“始终显示”不生效；未提供复现步骤、截图、浏览器或版本信息；无评论 | 先补齐复现条件，再核对 `card_description_mode` 的设置、归一化与详情卡片渲染链路，不直接假定是 CSS 或持久化问题 |
| [#19](https://github.com/lbjxr/CF-Navs/issues/19) 能不能把搜索引擎固定在最上方 | `—` / Open | 2026-09-14 09:47:49 | 2026-09-14 23:13:23 | 用户希望上下浏览书签时搜索入口仍可用；维护者初步考虑桌面端缩到右上角、移动端改为搜索图标 | 先与本地 `REQ-01` 对齐搜索入口、固定方式和响应式范围，再决定是否立项 |
| [#16](https://github.com/lbjxr/CF-Navs/issues/16) [Feature]: 建议增加管理员「跨设备轻量随手记 / 极速便签」功能 | `enhancement` / Open | 2026-09-12 13:28:46 | 2026-09-13 10:09:41 | 文本便签诉求较明确；同步、存储、备份和冲突语义待定；小图片是额外范围；另有评论认为功能可能增加臃肿 | 先决定做、暂缓或不做；若做，先锁文本范围与权限边界，图片单独裁定 |
| [#15](https://github.com/lbjxr/CF-Navs/issues/15) [Feature]: 兼容EdgeOne部署版本 | `enhancement` / Open | 2026-09-02 13:34:09 | 2026-09-14 01:51:52 | 当前标题已由模板占位更新为 EdgeOne 兼容请求；正文实际诉求仍未定义具体运行环境和能力范围；维护者尚未承诺排期；评论中的分类收缩建议未形成正式需求 | 由维护者澄清 EdgeOne 目标形态和最小兼容范围，再决定立项、暂缓或不支持 |

**建议处理顺序：#21 的复现与范围确认 → #19 的范围与 `REQ-01` 对齐 → #16 的产品范围裁定 → #15 的 EdgeOne 范围澄清。** 这是基于当前信息的整理建议，不是 GitHub 优先级或版本承诺；沟通可以独立推进。

## 2. #21：详情风格下始终显示描述不生效

### 已知事实与缺口

- Issue 正文只有一句「详情风格下，隐藏描述可以正常生效，但是始终显示不生效」，没有复现步骤、截图、浏览器/系统、版本、设置值或评论补充。
- 当前信息只能确认“隐藏”与“始终显示”两种描述策略的表现不同，不能据此断言设置没有保存、归一化错误、CSS 覆盖或卡片数据缺失。

### 建议任务

| 标记 | 任务与依赖 | 后续交付 / 验收条件 |
| --- | --- | --- |
| 21-A | 补齐详情卡风格、描述显示设置值、书签数据、浏览器/视口、预期与实际表现 | 两种策略都有可复现条件；确认问题发生在保存、读取、派生设置还是卡片渲染，不以单句描述指定根因 |
| 21-B | 对照 `card_description_mode`、旧 `card_show_description` 兼容归一化、设置表单和 `BookmarkCardInfo` 的显示分支 | `hidden`、`hover`、`always` 在刷新后仍保持各自语义；无描述与有描述书签、详情/极简风格边界均有证据 |
| 21-C | 在范围获准且根因确认后修复并验证原报告路径 | L0 通过；详情卡桌面/移动端真实浏览器均确认“始终显示”确实显示描述且不破坏隐藏/悬停模式 |

## 3. #19：搜索入口固定在顶部

### 已知事实与缺口

- Issue 正文希望搜索引擎固定在页面顶部，浏览上下页时不随页面滚动消失。
- 维护者评论提出初步方向：桌面端把搜索框缩小到右上角，移动端变成搜索图标。这是规划方向，不是已批准的 UI 契约。
- 仍需确定：是固定完整搜索框、紧凑搜索框还是仅按钮；是否复用或替代 `REQ-01` 的 Spotlight；与 `search_box_show` 设置、顶部导航和浮动操作行如何共存；键盘、触摸、窄屏和 reduced-motion 行为如何定义。

### 建议任务

| 标记 | 任务与依赖 | 后续交付 / 验收条件 |
| --- | --- | --- |
| 19-A | 维护者确认桌面端和移动端入口形态，并与 `REQ-01` 合并或拆分范围 | 有明确的产品范围、非目标和唯一搜索入口；不同时维护互相竞争的固定搜索框与 Spotlight 入口 |
| 19-B | 核对顶部导航、浮动操作行、`search_box_show` 和现有页面过滤状态的交互契约 | 固定入口不遮挡导航和内容；关闭主搜索框时仍有明确可发现入口；不改变现有页面过滤语义 |
| 19-C | 在范围批准后实现并验证 PC/移动端固定定位、键盘/触摸可达性和窄屏布局 | 真实浏览器下滚动、搜索、关闭、焦点恢复和响应式布局均有证据；不以静态源码存在替代交互验收 |

## 4. #16：管理员跨设备轻量便签

### 原始诉求、追加建议与维护者意见

正文明确提出：

1. 管理员登录后，在首页提供轻量「跨端随手记 / 便签」抽屉。
2. 支持多条文本备忘并自动保存，解决电脑与手机间临时文本、网址和备忘传递。
3. 每条提供一键复制，移动端通过点击写入系统剪贴板。
4. 建议复用 Cloudflare D1、增加 `notes` 表，不引入新服务；这是报告者的架构建议，尚未完成可行性确认。
5. 仅管理员鉴权可见，匿名访客完全屏蔽，不影响公开导航站性能。

评论追加了小容量图片传输建议；维护者表示会记录并规划实现方式，目前没有版本、日期或具体方案承诺。另有评论认为该功能可能没有必要，应作为产品取舍输入，不直接视为否决结论。

### 建议任务

以下验收是供立项裁定的建议，不是已经批准的产品契约。

| 标记 | 任务与依赖 | 后续交付 / 验收条件 |
| --- | --- | --- |
| 16-A | 确认文本便签范围与入口：管理员首页抽屉、PC/移动端操作；明确编辑/删除、条数/字数上限与排序需求 | 有明确范围与非目标；不默认引入富文本、协作编辑、分享、实时推送或离线同步；小图片留给 16-F 单独裁定 |
| 16-B | 评估 D1 数据和迁移方案；明确是否新增 `notes` 表、时间与更新标识、已有实例升级方式，以及是否纳入备份/恢复 | 存储方案不破坏现有分类/书签；备份范围、删除语义和迁移边界有结论；未经裁定不修改 schema |
| 16-C | 定义自动保存和跨设备可见性：保存触发、另一设备刷新时机、并发冲突、断网/失败反馈 | 保存中/成功/失败可区分；失败不丢输入；不能把“自动保存”直接等同于实时同步 |
| 16-D | 设计每条便签的一键复制，明确 PC/移动端点击路径和拒绝权限时反馈 | 复制结果保持换行和网址内容；只有剪贴板成功才提示成功；移动端限制需真实浏览器或真机验证 |
| 16-E | 明确鉴权、匿名隔离和公开性能边界 | 匿名和失效会话不能读写；正文不进入公开聚合、共享缓存或公开备份；匿名首页不新增便签请求或 D1 读取 |
| 16-F | 单独评估小图片传输 | 明确纳入或暂缓；若纳入，单独定义大小/数量、存储、访问权限和清理成本，不默认增加对象存储服务 |

**依赖：** 16-A 先裁定；16-B/16-C/16-E 共同确定保存、同步与权限契约；16-D 按已选界面落实；16-F 独立，不默认阻塞文本方案。批准开发后再按存储/API/UI 改动执行相应验证。

## 5. #15：EdgeOne 部署兼容

### 已知事实与既有决定

- 正文只有「能否开发兼容 EdgeOne 部署版本」，没有目标产品形态或功能范围。
- 维护者已表示目前没有计划，等下一个大版本完成后再纳入排期；不能改写成已承诺下个版本交付。
- 评论中的分类收缩建议已得到维护者现有导航能力的回应，没有形成独立产品契约。
- [BACKLOG](../BACKLOG.md) 的 `PROB-25` 已规定：由维护者直接在 GitHub 回帖澄清，不占用代理任务；澄清结果回来后再决定 `REQ-12` 是否排期。本轮未发送评论。

### 建议任务

| 标记 | 任务与依赖 | 后续交付 / 验收条件 |
| --- | --- | --- |
| 15-A | 澄清具体 EdgeOne 产品/运行环境、完整自托管还是仅静态前端、必须保留的认证/存储/导入导出能力 | 得到可评估的最小兼容范围及不支持项；回帖或改标题需另行授权 |
| 15-B | 范围明确后评估 Workers API、D1/KV 等价存储、认证与会话、静态资源、构建产物、部署配置/环境变量、CI 与文档差异 | 形成支持/替代/不支持矩阵及迁移风险；没有实际调研前不宣称平台可直接替换 |
| 15-C | 基于 15-B 决定立项、暂缓或不支持；若批准，再定义验证环境和交付边界 | 明确最小交付功能、部署/升级路径及验收方法；环境与部署需单独授权；`REQ-12` 是否排期以结果为准 |

## 6. 历史关闭 Issue

### #18：侧边栏子分类颜色与自定义强调色布局问题

- 当前状态：`Closed / Completed`，不属于当前 Open Issue 集合。
- 修复与归档记录见 [#18 关闭说明](https://github.com/lbjxr/CF-Navs/issues/18#issuecomment-5657852588)、[CHANGELOG](../../CHANGELOG.md) 的「侧边栏子分类颜色与自定义强调色布局修复（#18）」。
- 本快照不保留原 #18 的旧建议任务，避免把已完成问题重新列为活跃工作项。

## 7. 采集与后续使用

- 本次逐项读取 #21、#19、#16、#15 的当前正文和评论；活动 Open 集合为 `{21, 19, 16, 15}`。
- #18 仅作为 Closed / Completed 历史保留，不计入 Open 数量、活动建议顺序或当前建议任务数量。
- 执行任何任务前仍需重新读取相应 Issue 及最新评论；本快照不持续手写同步云端进度。
- 本轮只整理上述 **15 条活动建议任务**。具体功能是否开发、GitHub 沟通/状态更新、提交/推送与部署都不由本文自动授权。
