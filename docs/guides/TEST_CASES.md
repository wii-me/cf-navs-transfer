# CF-Navs 部署后验收测试用例

> **用途：** 用户将 `develop` 分支部署到自己的 Cloudflare Worker 后，按本文逐项手工验收。
>
> **当前状态：** 测试用例文档；待部署环境执行。本文不包含真实站点地址、管理员账号、密码、Token 或私有接口。
>
> **相关需求：** `docs/reference/GITHUB_ISSUES_REQUIREMENTS.md` R-01～R-08
>
> **测试原则：** 先做非破坏性用例；涉及导入覆盖、删除和改密码的用例必须使用测试数据或临时环境。每个用例执行后填写“实际结果”和“通过/失败”。

---

## 1. 测试记录

| 项目 | 填写内容 |
| --- | --- |
| 测试环境 URL | `________________________________________` |
| Worker 部署版本 / 时间 | `________________________________________` |
| 测试日期 | `________________________________________` |
| 浏览器及版本 | `________________________________________` |
| 桌面窗口尺寸 | 建议 `1440×1000` |
| 移动端模拟尺寸 | 建议 `390×844` |
| 测试账号 | 仅在本地记录，不写入代码或截图 |
| 测试人 | `________________________________________` |

### 结果标记

- **通过：** 实际结果符合预期。
- **失败：** 结果不符合预期，记录复现步骤、时间、页面 URL 和截图。
- **阻塞：** 因环境、权限、数据或外部服务无法执行；不得直接标记为通过。

---

## 2. 测试前准备

### 2.1 部署和安全准备

- [ ] 确认部署目标是 `develop` 对应的 Worker，不是 `main` 或其他环境。
- [ ] 确认 Cloudflare Worker 已绑定 D1 `DB` 和 KV `SESSION`。
- [ ] 首次安装环境已配置 `SETUP_TOKEN`；已安装环境不重复执行初始化。
- [ ] 使用测试管理员账号；不要在截图、日志或文档中记录密码和 Token。
- [ ] 如果已有正式数据，先通过备份功能保存完整备份。
- [ ] 导入 `replace`、批量删除、改密码用临时环境或可恢复测试数据。
- [ ] 浏览器打开 DevTools 的 Console 和 Network 面板。
- [ ] 测试期间记录：Console error、页面异常、失败请求和非预期 HTTP 4xx/5xx。

### 2.2 推荐测试数据

创建以下测试分类和书签。名称可按实际情况调整，但应保留层级和数量关系。

| 数据 | 建议内容 |
| --- | --- |
| 一级分类 A | `开发工具` |
| 一级分类 B | `资料` |
| 二级分类 A1 | `前端`，父级为 `开发工具` |
| 二级分类 A2 | `后端`，父级为 `开发工具` |
| 一级直属书签 | 至少 2 条，归属 `开发工具` |
| A1 书签 | 至少 3 条，归属 `前端` |
| A2 书签 | 至少 2 条，归属 `后端` |
| B 书签 | 至少 2 条，归属 `资料` |
| 长标题书签 | 标题和描述均较长 |
| 图标测试书签 | 分别使用 HTTP(S) 图标、文字/表情图标、Iconify 图标 |
| 私密数据 | 1 个私密分类或私密书签，仅用于公开模式测试 |

为了验证跨页选择，后台书签数量建议达到 11 条以上；为了验证空目标，保留一个没有书签的分类。

---

## 3. P0 部署与基础可用性

### TC-DEP-01 健康检查

**前置条件：** Worker 已完成部署。

**步骤：**

1. 打开 `https://<你的部署地址>/api/health`。
2. 查看响应。

**预期结果：**

- HTTP 200。
- 响应 `code=0`。
- `data.status` 为 `ok`。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-DEP-02 首页加载

**步骤：**

1. 打开站点根路径。
2. 等待首屏加载完成。
3. 强制刷新一次页面。
4. 查看 Console 和 Network。

**预期结果：**

- 首页正常显示，无安装错误、白屏或持续加载。
- 页面标题、搜索框、导航区域和分类内容正常显示。
- 无未处理的 Console error 或页面异常。
- 不出现非预期 4xx/5xx 请求。
- 新版 Service Worker 接管后，刷新仍能显示当前版本内容。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-DEP-03 首次安装（仅全新环境执行）

**步骤：**

1. 打开 `/install`。
2. 输入部署时配置的 `SETUP_TOKEN`。
3. 设置测试管理员账号和密码。
4. 完成安装并登录。
5. 再次打开 `/install`。

**预期结果：**

- 安装成功后进入登录态首页。
- `/install` 初始化数据库和管理员账号。
- 再次安装被拒绝，不会覆盖已有管理员或数据。
- 安装完成后删除或轮换 `SETUP_TOKEN`。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 不适用

### TC-AUTH-01 登录、后台入口和退出

**步骤：**

1. 退出登录，刷新首页。
2. 使用正确账号密码登录。
3. 点击右上角管理入口进入后台。
4. 点击退出登录。
5. 刷新页面并尝试访问后台。

**预期结果：**

- 正确账号可以登录。
- 登录后回到前台首页，管理入口可以进入后台。
- 退出后本地登录态被清除。
- 退出后访问受保护数据被拒绝，通常为 HTTP 401、`code=1001`。
- 不显示管理员密码或内部凭据。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 4. R-01～R-04 首页分类和书签操作

### TC-R01-01 桌面跨分类排序：一级到二级

**前置条件：** 已登录；存在一级分类 A、二级分类 A1，且两边都有书签。

**步骤：**

1. 在桌面首页进入排序模式。
2. 将一级分类 A 下的书签拖到二级分类 A1。
3. 检查排序草稿中的来源和目标列表。
4. 点击取消排序。
5. 再次进入排序模式，执行相同移动并保存。
6. 刷新首页和后台书签列表。

**预期结果：**

- 首页同时显示所有一级分类分组。
- 排序模式显示完整排序列表，而不是只显示当前分页。
- 取消后书签恢复原分类和原顺序。
- 保存后书签归属变为 A1，顺序正确。
- 刷新后分类归属和顺序仍保持。
- 保存期间若发生冲突，应整体失败并恢复服务端数据，不出现部分成功。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R01-02 桌面移动到空分类

**前置条件：** 存在没有书签的目标分类。

**步骤：**

1. 进入首页排序模式。
2. 将任意书签拖入空分类的 drop zone。
3. 保存并刷新。

**预期结果：**

- 空分类在排序模式下仍提供可放置区域。
- 保存成功后书签归属为空分类。
- 原分类不再显示该书签。
- 刷新后归属保持。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R01-03 移动端“移动到分类”菜单

**前置条件：** 使用 `390×844` 或真实手机；已登录；存在可移动书签。

**步骤：**

1. 打开首页，普通浏览状态下观察书签卡片右上角是否有三点按钮。
2. 长按（或右键）书签卡片，确认能打开编辑入口。
3. 关闭菜单，点击分类区域的“排序”按钮进入排序模式。
4. 确认排序模式下书签卡片右上角出现移动图标入口（区别于三点/加号）。
5. 点击移动入口，打开分类树后，在树内用鼠标滚轮或触摸上下拖动，访问后续分类，再选择二级分类或空分类。
6. 在排序浮窗中观察说明文字、取消按钮和保存排序按钮的排列，以及“回到顶部”按钮是否与浮窗重叠。
7. 保存并刷新页面。

**预期结果：**

- 普通浏览状态下书签卡片不显示三点按钮；长按/右键仍可打开编辑。
- “移动”入口仅在排序模式下出现，使用移动图标。
- 移动端不要求使用触控拖拽；排序模式下的移动菜单可完成移动，打开菜单或分类树后不再触发书签卡片拖拽。
- 分类树可以展开父分类；树内容超出高度时，鼠标滚轮/触摸拖动只滚动分类树，不穿透滚动书签列表。
- 排序浮窗左右贴合移动端可用宽度，不按 PC 方式居中留出两侧空白。
- 说明文字单独位于第一行；取消和保存排序按钮可以在第二行并排显示。
- 排序浮窗显示时“回到顶部”按钮上移避让，两者不重叠。
- 成功反馈明确，书签移动后分类和顺序正确，刷新后保持。
- 菜单、弹层和排序浮窗不超出视口，不被其他固定控件遮挡。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R02-01 首页新建子分类

**步骤：**

1. 登录后在首页选择一级分类 A。
2. 点击该分组的“新建子分类”入口。
3. 检查分类弹窗中的父级默认值。
4. 修改名称并保存。
5. 返回首页观察分类导航和内容区域。

**预期结果：**

- 只有登录管理员看到新建入口，移动端按钮显示可识别的加号图标而不是空白方块。
- 弹窗父级默认预填当前一级分类，但仍可修改。
- 创建成功后返回首页。
- 新二级分类自动进入当前一级分类作用域，并被选中、滚动到可见位置和高亮。
- 移动端入口尺寸和弹窗内容均在视口内。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R03-01 首页新增主分类

**步骤：**

1. 登录后打开首页的分类新增入口。
2. 选择“新增主分类”。
3. 确认父级为“无上级分类”。
4. 保存新分类。
5. 刷新首页。
6. 退出登录后再次检查入口。

**预期结果：**

- 新增主分类入口仅对管理员显示。
- 父级默认为“无上级分类”。
- 创建成功后新一级分类可见，并自动定位/高亮。
- 匿名用户看不到管理员新增入口。
- 移动端入口不遮挡内容、不超出视口。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R04-01 编辑书签时定位当前分类

**前置条件：** 存在归属于二级分类 A1 的书签。

**步骤：**

1. 从首页或后台打开该书签的编辑弹窗。
2. 展开分类选择器。
3. 观察分类树展开状态、滚动位置和选中状态。
4. 不修改分类，直接取消。
5. 再次编辑并切换到其他分类后保存。

**预期结果：**

- 弹窗打开后自动展开当前二级分类的一级父级。
- 当前分类自动滚动到可见区域并高亮。
- 当前选项具有 `aria-selected=true` 或等效可访问选中状态。
- 未修改分类时取消不会改变书签归属。
- 当前分类已经不存在时显示“当前分类不可用”，不静默改写表单值。
- 桌面和 `390×844` 移动端均可操作。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 5. R-05 后台批量移动

### TC-R05-01 跨页选择

**前置条件：** 后台书签数量超过一页。

**步骤：**

1. 进入后台书签列表。
2. 在第 1 页选择 10 条书签。
3. 翻到第 2 页，再选择至少 1 条。
4. 返回第 1 页。
5. 观察批量操作工具栏和选择状态。

**预期结果：**
- 已选数量跨分页保留。
- 工具栏显示类似“已选 11 项（跨 2 页）”。
- 批量操作栏为独立浮层，不挤压书签列表：PC 列表不因工具栏出现而被压缩产生额外竖向滚动条；移动端列表上方不出现空白区。
- PC 浮层居中显示，移动端浮层位于底部导航上方，均不遮挡三个批量操作按钮。
- 返回上一页后原选择仍保持。
- 清空选择只清除当前批量选择，不修改服务器数据。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R05-02 批量移动到二级分类并追加到末尾

**步骤：**

1. 选中跨页书签。
2. 点击批量移动。
3. 在目标树中展开一级分类 A，选择二级分类 A1。
4. 选择“追加到末尾”。
5. 确认移动。
6. 刷新后台和首页。

**预期结果：**

- 目标树显示完整分类路径。
- 确认弹层显示目标分类、移动数量和位置。
- 成功后所有选中书签归属 A1，并追加到末尾。
- 返回数量与选中数量一致。
- 列表刷新后选择状态清理，数据归属保持。
- PC 工具栏不被书签列表遮挡；移动端工具栏位于底部导航上方且不被遮挡，弹层完整可见。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R05-03 批量移动并插入顶部

**步骤：**

1. 重新选择至少 2 条书签。
2. 选择另一个目标分类。
3. 选择“插入顶部”。
4. 确认并刷新列表。

**预期结果：**

- 书签进入目标分类顶部。
- 目标分类原有书签顺序相对关系保持。
- 选中项之间的顺序符合页面显示/确认前顺序。
- 不影响其他分类书签。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R05-04 冲突时整体失败

**前置条件：** 准备两个管理员浏览器会话或一个页面加一个 API 调用窗口；使用可恢复测试数据。

**步骤：**

1. 会话 A 选择要批量移动的书签，打开确认弹层但暂不提交。
2. 会话 B 先移动其中一条书签，或修改相关排序。
3. 回到会话 A 提交批量移动。
4. 检查响应、列表和数据库中的全部书签。

**预期结果：**

- 请求返回冲突提示，通常为 `code=1006`。
- 整批移动失败，不出现部分成功。
- 所有书签归属和顺序与会话 B 的最新服务端状态一致。
- 前端刷新或恢复服务端数据后，不保留过期草稿。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 6. R-06 分类视觉设置

### TC-R06-01 一级/二级字号和图标尺寸

**步骤：**

1. 进入“站点设置 → 外观与卡片”。
2. 确认“分类标题字体与图标”卡片默认收纳在“高级设置”中。
3. 展开高级设置，分别调整一级标题字号、一级图标尺寸、二级标题字号、二级图标尺寸。
4. 查看右侧实时预览。
5. 保存并刷新首页。
6. 在分类标题较多时，用鼠标拖动横向滚动条、滚轮、Shift+滚轮或键盘 Home/End/左右键访问后续二级分类标题。
7. 切换左侧导航、顶部导航和搜索结果。
8. 使用 `390×844` 再检查一次。

**预期结果：**

- 分类标题字体与图标卡片默认收纳在“高级设置”；高级设置收起时不占用常规设置区域。
- 一级和二级设置互相独立，一级设置不会直接覆盖二级设置。
- 首页标题、二级标签、搜索分组、顶部/左侧导航均同步变化。
- PC 二级分类标题列表可通过横向滚动条、鼠标滚轮/Shift+滚轮和键盘访问完整内容；调大字号或图标后，后续标题仍可达。
- 移动端使用统一 0.88 派生比例，不需要单独填写一套移动端数值。
- 长标题、无图标、图片图标、文字/表情图标不重叠、不横向溢出。
- 未保存修改只影响实时预览，不直接写入线上数据。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R06-02 设置保存和失败保护

**步骤：**

1. 修改分类视觉设置但不保存，刷新设置页或取消编辑。
2. 确认未保存值没有错误覆盖已保存值。
3. 在可控的网络失败环境下提交一次设置，或观察保存失败提示。
4. 恢复网络后重新保存。

**预期结果：**

- 保存成功后刷新页面仍保持新设置。
- 保存失败时显示错误提示，旧设置仍可恢复。
- 不因保存失败清空其他设置项。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 7. R-07 卡片宽度和卡片风格

### TC-R07-01 详情卡片宽度边界

**步骤：**

1. 进入“站点设置 → 外观与卡片 → 高级设置”。
2. 确认详情卡片列宽下限控件 `min=40`、`max=400`。
3. 依次输入并保存 `39`、`40`、`44`、`79`、`80`、`400`、`401`。
4. 每次保存后刷新首页。
5. 桌面和 `390×844` 移动端分别观察长标题、描述、图标和点击区域。

**预期结果：**

- `39` 和更小值保存/读取后统一归一化为 `40`。
- `40～68` 可以保存，并显示“当前宽度下详情卡只显示图标：标题与描述的可用宽度为 0…”提示；`69～79` 显示“当前宽度低于 80 px，标题与描述会被截断…”提示；`40` 与 `44` 都按输入值生效，不再被钳到 44。
- `80` 正常保存。
- `400` 可以保存。
- `401` 归一化为 `400`。
- 详情 Grid 的实际列宽随配置变化：低宽度可形成更多列，高宽度减少列数。
- **桌面 40 px 实测（2026-09-05）**：详情卡只显示图标——标题与描述的 `clientWidth` 为 `0`；标题约需 `120 px` 才完整显示，描述到 `150 px` 仍被截断。这是预期行为而非缺陷，控件提示已如实说明。
- 移动端保留可读性安全下限：`390×844` 下 `--mobile-card-min-width` 实测为 `150px`、每行 2 张、卡片 `171×60`，标题与描述完整显示，`scrollWidth - clientWidth = 0` 无横向溢出。
- 卡片仍可点击，标题/描述不会突破卡片边界。移动端点击区域 `171×60` 满足 44 px；桌面 40 px 档宽度方向小于 44 px，属已接受取舍。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R07-02 极简卡片宽度联动

**步骤：**

1. 将卡片风格切换为“极简”。
2. 观察详情卡片列宽下限控件。
3. 修改极简卡片图标大小，保存并刷新首页。
4. 切换是否显示图标标题。

**预期结果：**

- 极简风格的卡片宽度控件置灰，不能单独修改。
- 页面说明卡片大小由图标尺寸决定。
- 修改图标尺寸后，极简卡片轨道宽度同步变化。
- 图标标题显示/隐藏时，卡片不会重叠或横向溢出。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 8. R-08 部分导出和导入

> **注意：** `replace` 会清空目标环境现有分类和书签。必须使用临时环境、测试数据或已确认可恢复的备份。

### TC-R08-01 一级分类子集导出

**步骤：**

1. 进入后台备份/导出面板。
2. 只选择一级分类 A。
3. 关闭“包含设置”（如页面提供该选项）。
4. 查看导出前汇总并执行下载。
5. 解压/打开 JSON，检查 `categories` 和 `bookmarks`。

**预期结果：**

- 导出前显示分类数和书签数。
- 包含一级分类 A 必要的子分类和对应书签。
- 不包含未选中兄弟分类或其书签。
- 下载文件是合法 JSON，分类和书签引用关系完整。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R08-02 二级分类子集导出与父分类补全

**步骤：**

1. 清空选择。
2. 只选择二级分类 A1。
3. 查看汇总并下载。
4. 检查导出 JSON。

**预期结果：**

- 导出结果包含 A1 和恢复层级所需的一级父分类 A。
- 不额外包含 A2 或其他兄弟分类。
- 只包含 A1 对应书签。
- 下载反馈中的分类数、书签数与文件内容一致。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R08-03 全选、清空和空选择

**步骤：**

1. 点击全选，检查汇总并下载。
2. 点击清空，检查汇总。
3. 在没有选中任何分类时尝试导出。
4. 在移动端重复全选/清空操作。

**预期结果：**
- 全选包含全部分类和书签。
- 清空后汇总归零。
- 空选择被拦截并显示明确提示，不下载空文件。
- 移动端分类树可滚动，导出按钮位于分类树下方、导入数据卡片上方的正常流式全宽布局，不全屏悬浮。
- 导出按钮不遮挡分类树最后一项，不出现横向溢出。

**实际结果：**

`____________________________________________________________`


**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R08-06 移动端导出按钮布局

**前置条件：** 移动端（≤700px）登录后台并打开“数据备份与导入”，准备至少一个可导出的分类。

**步骤：**

1. 在移动端观察导出按钮相对分类树和“导入数据”卡片的位置。
2. 滚动备份面板，观察导出按钮是否随页面滚动。
3. 检查按钮宽度和是否有横向溢出。

**预期结果：**

- 导出按钮位于分类树下方、“导入数据”卡片上方，属正常流式布局，随页面滚动，不全屏固定悬浮。
- 导出按钮为全宽显示，不遮挡分类树或导入卡片。
- 无横向溢出，分类树最后一项可访问。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R08-04 replace 回导

**前置条件：** 使用临时环境或已备份的测试数据库。

**步骤：**

1. 准备包含一级分类、二级分类、书签和设置的 JSON。
2. 选择覆盖/`replace` 模式导入。
3. 确认覆盖警告。
4. 导入完成后检查分类、书签和设置。

**预期结果：**

- 导入前明确提示覆盖风险。
- 分类层级恢复正确。
- 书签 `category_id` 指向正确分类。
- 受支持的公开设置按预期应用。
- 管理员账号和管理员密码不被备份文件覆盖。
- 导入完成后返回实际导入数量和最新数据。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-R08-05 merge 回导

**前置条件：** 目标环境已有与导入文件部分同名的分类。

**步骤：**

1. 准备包含已有分类、全新分类、父子分类和书签的 JSON。
2. 选择合并/`merge` 模式导入。
3. 确认导入。
4. 检查分类复用、新建、书签归属和设置。

**预期结果：**

- 相同完整分类路径被复用，不重复创建。
- 新分类追加创建，父子层级正确。
- 重复 URL 按当前契约处理，不因重复 URL 导致整批失败。
- 合并模式不覆盖目标站点原有设置。
- 返回复用分类、新建分类、跳过书签等统计。
- 管理员账号保护保持有效。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 9. 搜索、导航、图标和访问分析回归

### TC-GEN-01 搜索和分类导航

**步骤：**

1. 在搜索框输入书签标题关键词。
2. 输入 URL 关键词、描述关键词和父分类名称。
3. 点击二级分类标签。
4. 清空搜索框。
5. 切换左侧导航和顶部导航布局。

**预期结果：**

- 搜索直接显示全站分类分组结果，不出现旧式本地下拉列表。
- 搜索父分类名称可以命中后代书签，并保留必要的祖先结构。
- 清空后恢复各一级分组此前的分类选择。
- 顶部导航固定但不遮挡标题、搜索框和分类内容。
- 分类导航溢出时桌面箭头/拖动、移动端横向滑动可用。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-GEN-02 图标和失败回退

**步骤：**

1. 创建或编辑不同图标来源的书签和分类。
2. 观察首页、搜索分组、顶部导航和左侧导航。
3. 在 Network 面板检查图标请求。
4. 使用一个不可访问的图标地址测试失败回退。

**预期结果：**

- HTTP(S)、文字、表情、data URI、Iconify 图标均能显示。
- 图片加载失败时显示稳定的文字回退，不破坏布局。
- 编辑弹窗中的 Iconify 预览使用同源 `/api/iconify/*` 代理。
- 普通已缓存图标滚动、刷新和页面切换时不重复产生不必要请求。
- 普通图标缓存失败时，首页仍可使用已保存图标 URL 或文字回退。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-GEN-03 点击次数和访问分析

**步骤：**

1. 首页打开至少 2 个书签。
2. 进入后台“访问分析”。
3. 查看总点击、已访问、零访问、Top 20 和零访问分页。
4. 返回首页再打开一个书签，重新进入访问分析。

**预期结果：**

- 点击书签后点击次数正常增加。
- 访问分析进入时刷新最新数据。
- 已访问和零访问统计互相一致。
- Top 20 排名和零访问分页可操作。
- 重复快速点击受限流保护时接口仍不报错，不异常增加计数。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-GEN-04 公开模式和私密数据

**步骤：**

1. 登录后台开启公开模式并保存。
2. 退出登录，刷新首页。
3. 验证公开分类和书签。
4. 将公开模式关闭并保存。
5. 退出登录再次访问。
6. 检查私密分类/书签在登录前后是否符合预期。

**预期结果：**

- 公开模式开启时匿名用户可以读取公开内容和使用搜索。
- 私密分类及其后代内容不向匿名用户泄露。
- 公开模式关闭时匿名聚合数据被拒绝，通常为 `code=1005`；页面显示登录入口而不是内部错误。
- 管理员登录后仍可管理全部数据。
- 公开响应不包含管理员密码、内部账号等敏感字段。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 10. 设置预览、响应式和可访问性

### TC-UI-01 设置实时预览

**步骤：**

1. 进入“站点设置”。
2. 修改主题、首页标题、卡片风格、描述策略、导航布局、背景和分类视觉设置，但先不保存。
3. 观察右侧首页预览。
4. 在预览中切换浅色/深色。
5. 检查自定义 CSS、页脚 HTML 和自定义脚本预览。

**预期结果：**

- 预览实时反映未保存设置。
- 预览覆盖标题、搜索、分类、卡片、布局和主题变化。
- 预览中的自定义 CSS/页脚 HTML 在隔离环境渲染。
- 预览不执行自定义脚本、不写入真实数据、不发起业务 API 请求。
- 取消或离开未保存页面后，线上设置不被意外覆盖。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

### TC-UI-02 桌面/移动端布局

**步骤：**

1. 使用桌面宽屏、窄桌面和 `390×844` 移动尺寸。
2. 检查首页、后台列表、设置页、导出面板和各类弹窗。
3. 通过键盘 Tab、Shift+Tab、Enter、Space、Escape 操作主要控件。

**预期结果：**

- 页面无横向溢出，主要内容不被固定工具栏遮挡。
- 移动端侧边栏使用抽屉；顶部分类导航可横向滑动。
- 后台批量操作栏、导出 CTA 和确认弹层均在视口内。
- 可见焦点清晰，按钮和输入框可通过键盘操作。
- 弹窗打开后焦点位置合理，Escape 可关闭可取消弹窗。
- 按钮、卡片链接和菜单有可理解的名称或 aria-label。

**实际结果：**

`____________________________________________________________`

**结果：** [ ] 通过 [ ] 失败 [ ] 阻塞

---

## 11. 浏览器 Network / Console 检查表

每轮测试结束后记录以下结果：

| 检查项 | 预期 | 实际结果 |
| --- | --- | --- |
| Console error | 0 | `________________` |
| 页面未捕获异常 | 0 | `________________` |
| Network failed request | 0 | `________________` |
| 非预期 HTTP 4xx/5xx | 0 | `________________` |
| `/api/admin/data` | 登录后台时正常请求；刷新优先使用快照时不应无条件重复请求 | `________________` |
| `/api/icon/*` | 已缓存普通图标不因滚动/切换重复请求 | `________________` |
| `/api/iconify/*` | 后台候选和预览使用同源代理 | `________________` |
| `/api/category-icon/*` | HTTP(S) 分类图片请求正常，失败有文字回退 | `________________` |
| `/api/data/version` | 页面启动版本检查正常 | `________________` |
| 敏感响应字段 | 不出现密码、Token、Cookie 内容 | `________________` |

---

## 12. 失败记录模板

复制下面模板记录每个失败用例：

```text
用例编号：
测试环境：
部署版本 / 时间：
浏览器及窗口尺寸：
前置数据：
复现步骤：
预期结果：
实际结果：
页面 URL：
HTTP 状态 / code：
Console 错误：
失败请求：
截图或日志位置（不得包含密码/Token）：
是否可稳定复现：
```

---

## 13. 验收结论

| 范围 | 通过数 | 失败数 | 阻塞数 | 结论 |
| --- | ---: | ---: | ---: | --- |
| 部署与认证 |  |  |  |  |
| R-01～R-04 首页操作 |  |  |  |  |
| R-05 批量移动 |  |  |  |  |
| R-06 分类视觉设置 |  |  |  |  |
| R-07 卡片宽度 |  |  |  |  |
| R-08 部分导出/导入 |  |  |  |  |
| 搜索、图标、分析 |  |  |  |  |
| 响应式与可访问性 |  |  |  |  |

**最终结论：** [ ] 通过 [ ] 有条件通过 [ ] 不通过

**剩余问题 / 后续动作：**

`__________________________________________________________________`

`__________________________________________________________________`

> 部署到 `develop` 后的测试结果只代表该部署环境；不能据此自动声称 GitHub Issue 已关闭，也不能替代生产环境的独立验收。
